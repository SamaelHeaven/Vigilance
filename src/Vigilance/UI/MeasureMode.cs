namespace Vigilance.UI;

public enum MeasureMode : byte
{
    Undefined = FlexLayoutSharp.MeasureMode.Undefined,
    Exactly = FlexLayoutSharp.MeasureMode.Exactly,
    AtMost = FlexLayoutSharp.MeasureMode.AtMost,
}
