namespace Vigilance.UI;

public enum MeasureMode : sbyte
{
    Undefined = FlexLayout.MeasureMode.Undefined,
    Exactly = FlexLayout.MeasureMode.Exactly,
    AtMost = FlexLayout.MeasureMode.AtMost,
}
