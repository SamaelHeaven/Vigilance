<Project>
  <PropertyGroup>
    <LibsPath>$([MSBuild]::NormalizeDirectory('$(MSBuildThisFileDirectory)libs\'))</LibsPath>
    <FreetypePath>$(LibsPath)freetype.a</FreetypePath>
    <RaylibPath>$(LibsPath)raylib.a</RaylibPath>
  </PropertyGroup>
</Project>
